%h2
  Listings
- if @listings
  - @listings.each do |l|
    .listing
      %a{ :href => "/admin/listings/#{l.id}" }= l.starting_at.strftime("%Y-%m-%d")
      %br
      %span.visible= "visible: #{l.page.visible}"
      %br
      %br
      %br
- else
  %p
    No listings.
